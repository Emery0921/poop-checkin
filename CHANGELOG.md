# 更新日志

拉屎打卡（Poop）的版本记录，按时间倒序。日期为提交日期，括号内为 commit。

## 未提交（开发中）

暂无。

## 2026-08-20 称号系统 + 称号图鉴 + 更新日志弹窗

- 新增三槽位称号体系，全部由现有数据推导，无需改动数据库
  - 主线称号：按累计打卡次数解锁，拿到不会掉（新手 1 次 → 通渠 3 → 热身 5 → 准时 7 → 稳定 10 → 大师 15 → 火箭 20 → 传奇 25 → 神龙 30 → 顿悟 60）
  - 支线称号：按当前连续天数评定（3/5/7/10/14 天），连续两天未打卡则转为断更称号（2/3/5/7 天），两者互斥
  - 时段称号：按打卡时间点最集中的时段评定，累计满 5 次才给
- 排行榜行同时展示三个称号；超过两个时称号单独占一行，避免撑爆行宽
- 移除「成就徽章」页签（仅自己可见、与称号重复），替换为「称号图鉴」，按主线/连续/断更/时段分四个子页签展示解锁条件，并标注当前持有的称号
- 门槛阈值统一由 `src/lib/utils.ts` 的规则数组导出，图鉴只读取不重复声明
- 新增更新日志弹窗：老用户在版本号变化后首次进入弹一次，点「知道了」写入 localStorage 不再弹；新用户注册时直接标记为已读
- `ConfirmModal` 的 `onCancel` 改为可选，不传时只渲染确认按钮，供纯告知类弹窗复用
- 补充本更新日志文档

## 2026-08-17 每周一次补卡 + 按周排行榜（593304c）

- 补卡：每周（自然周，周一重置）可补一个过去未打卡的日期，`checkins` 新增 `is_makeup` 字段；配额按补卡发生时间统计，配额查询失败时按已用完处理
- 排行榜：新增周榜并设为默认视图，可切换总榜，可查看最近 8 周历史；分享文案与成就仍基于总榜
- 日期运算统一改为 UTC 锚定，修复设备时区非东八区时星期计算错误导致的周分组错乱

## 2026-08-12 排行榜展示今日打卡的具体时间点（8186f78）

- `getRanking` 收集每个用户今日打卡的时间戳（`todayTimes`）
- 排行榜「今日已打卡」标记后展示具体打卡时间，与连续天数同行
- `todayCount` 改为 `todayCheckins`，保留完整记录用于打卡按钮下方的时间信息

## 2026-08-11 找回码功能支持跨设备恢复身份（75a86ca）

- `users` 表新增 `recovery_code` 字段（房间内唯一）
- 加入房间时生成 8 位找回码，弹窗展示并支持一键复制
- 注册弹窗新增「用找回码恢复身份」入口
- 提取 `ConfirmModal` 公共组件，复用于打卡确认与找回码展示

## 2026-08-11 修复用户被删除后打卡无响应、注册按钮防抖（4443718）

- 打卡时若检测到外键约束失败（用户已被删除），清空本地身份并重新弹出注册弹窗
- 注册弹窗新增 loading 状态，加入中禁用按钮防止重复提交

## 2026-08-11 排行榜显示今日已打卡标记（46f370e）

- `getRanking` 计算每个用户今天是否已打卡，列表中展示 ✅ 标记

## 2026-08-05 打卡二次确认弹窗 + 3 分钟内可撤回（c72dbe3）

- 打卡前弹出二次确认
- 打卡后 3 分钟内可撤回（客户端计时，`UNDO_DURATION = 180`）

## 2026-08-05 iOS 兼容性修复（a19207a）

- 旧版 WebKit 缺少 `crypto.randomUUID` 时降级为手动生成 UUID

## 2026-08-05 首个可用版本 MVP（a9b2621）

- 一天可多次打卡
- 排行榜（累计次数 + 连续天数）
- 日历热力图
- 成就徽章（已于后续版本移除）
- 技术栈：Vite + React 19 + TypeScript + Tailwind CSS v4 + Supabase（浏览器直连 anon key）
